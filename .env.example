# Backend hazır olduğunda (uvicorn app.main:app --reload) varsayılan port 8000'dir
VITE_API_BASE_URL=http://localhost:8000/api

# true  -> tüm API çağrıları mock veriyle çalışır (backend olmadan test)
# false -> gerçek backend'e bağlanır (feature/backend-api branch'i)
VITE_USE_MOCK=true
